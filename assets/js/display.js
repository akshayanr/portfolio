function noDisplayName() {
    // get the clock
    var span = document.getElementsByTagName('tagLine')[0];

    // get the current value of the clock's display property
    var displaySetting = span.style.display;

    displaySetting = 'none';
}